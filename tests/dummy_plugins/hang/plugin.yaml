plugin_id: hang-plugin
version: 1.0.0
api_version: 1
entrypoint:
  command:
    - bin/plugin
  protocol: stdin_stdout_json
runtime:
  mode: subprocess
  timeout_seconds: 1
permissions:
  capabilities:
    - plugin:test
