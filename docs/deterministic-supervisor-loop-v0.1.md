# Deterministic Supervisor Loop v0.1 (DSL v0.1)

## Purpose

Define a proactive, deterministic supervisor that selects work from Git metadata (milestones, issues, optional task files) and executes it through agents with atomic claiming, idempotent execution, and complete audit logging.
